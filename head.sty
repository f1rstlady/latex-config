\RequirePackageWithOptions{scrlayer-scrpage}
\pagestyle{scrheadings}

% Include the head into the text body
\KOMAoptions{headinclude=true}
\recalctypearea

% Rule to separate the head
\setheadsepline{0.7pt}
