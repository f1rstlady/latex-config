\RequirePackageWithOptions{scrlayer-scrpage}
\pagestyle{scrheadings}

% Include the foot into the text body
\KOMAoptions{footinclude=true}
\recalctypearea

% Rule to separate the foot
\setfootsepline{0.7pt}
