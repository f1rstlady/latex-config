%
% Miscellaneous settings, that are usually used.
%

% Identification
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{miscellaneous}[2019/08/08 Miscellaneous settings]

% Options
\DeclareOption*{
    \PackageWarning{miscellaneous}{Unknown option '\CurrentOption'}
}

\ProcessOptions*

% Colors
\RequirePackage[svgnames]{xcolor}

% Use shortcuts for specifying the labelling of enumerations. When the beamer class is used, do not
% activate this since it uses the enumitem package which conflicts which beamer's bulleting and
% enumeration style.
\makeatletter
\@ifclassloaded{beamer}
  {}
  {\RequirePackage[shortlabels]{enumitem}}
\makeatother

% Lists and enumerations with multiple rows or columns
\RequirePackage{multirow}
\RequirePackage{multicol}

% Framed, shaded and similarly outlined text boxes
\RequirePackage{framed}

% Integration of PDF files
\RequirePackage{pdfpages}

% To put pages in landscape
\RequirePackage{pdflscape}

% Integration of graphics
\RequirePackage{graphicx}

% Verbatim-like environments in which \, { and } keep their usual meaning
\RequirePackage{alltt}
