% Use shortcuts for specifying the labelling of enumerations and enable inline lists, but only if
% the document class is not beamer.
\@ifclassloaded{beamer}{}{\RequirePackage[shortlabels,inline]{enumitem}}

% Give the source of an image in tiny, grey text
\newcommand*{\source}[1]{\textcolor{grey!40}{\tiny{Quelle: #1}}}

% Tables with proper spacing, without vertical rules and double rules
\RequirePackage{booktabs}
