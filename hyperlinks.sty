\RequirePackage{etoolbox} % hyperref has to be loaded last
\AtEndPreamble{
    \RequirePackage{hyperref}
    \hypersetup{ unicode % Unicode encoding of PDF strings
               , bookmarksnumbered % Include section and chapter numbers in the bookmarks
    }}
