%
% Custom layout.
%

% Options
\DeclareOption*{
    \PassOptionsToPackage{\CurrentOption}{geometry}
}

\ProcessOptions*

% Use the geometry package with A4 as paper size and layout as default
\RequirePackage[paper=a4paper,layout=a4paper]{geometry}
